// const express = require('express');
// const router = express.Router();
// const supertest = require('supertest');
// const request = supertest(router);
// const sinon = require('sinon');

// const jest = require('jest');

jest.mock('../services/booksService');

// describe('BooksRoute Tests', function () {
//   it('responds to /books/latest', () => {
//     const req = {};
//     const res = {
//       book: { name: 'Tuck Everlasting', year: 1990 },
//       send: (input) => {
//         this.book = input;
//       },
//     };
//     booksService.retrieveLatest(req, res);
//     expect(res.book.name).to.equal('Tuck Everlasting');
//   });
// });

it('Testing to see if Jest works', () => {});
